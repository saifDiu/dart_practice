void main(){
  const name = 'admin'; //constant varible declaration can work on compile time and be changed.
  print(name);
  final state = 'done'; //final declaration can happen only one time and never be changed.
  print(state);
}